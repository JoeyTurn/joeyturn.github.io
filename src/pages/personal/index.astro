---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import PersonalDropdowns from "../../components/PersonalDropdowns.astro";
const posts = (await getCollection("blog")).filter(p => !p.data.draft);
---
<BaseLayout title="Joey Turnbull - Personal">
  <h1>Personal</h1>
  <!-- <ul>
    {posts.map(({ slug, data }) => (
      <li><a href={`/personal/${slug}/`}>{data.title}</a></li>
    ))}
  </ul> -->
  Hi! I'm putting a potpourri of non-reasearch thoughts/updates/more personal items here!

  <PersonalDropdowns />

</BaseLayout>
